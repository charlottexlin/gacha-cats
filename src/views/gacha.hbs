<!-- Gacha page -->
<div class="container-fluid text-center">
    <!-- Display error message -->
    {{#if errorMsg}}
        <div class="alert alert-danger sticky-top">
            {{ errorMsg }}
        </div>
    {{/if}}
    <!-- Gacha machine image -->
    <img src="/img/gachapon.png" alt="Gacha machine" class="img-fluid">
    <br>
    <!-- Link to roll -->
    <a href="/gacha/roll" class="btn btn-secondary mb-3">ROLL! (💰10)</a>
    <!-- Player's coin count -->
    <p class="h4">You currently have: 💰{{user.coins}}</p>
</div>