<head>
    <!-- REFERENCE: https://www.w3schools.com/html/html_favicon.asp -->
    <link rel="stylesheet" href="/css/styles.css" media="screen">
    <link rel="icon" type="image/x-icon" href="/img/favicon.ico">
    <div class="container-fluid text-center">
        <div class="row justify-content-md-center">
            <div class="col col-lg-4"></div>
            <div class="col col-lg-4">
                <img src="/img/logo.png" alt="Gacha Cats!" class="img-fluid">
            </div>
            <div class="col col-lg-1 mt-5">
                {{#if user}}
                    <form method="POST" action="logout">
                        <input type="submit" value="Log out" class="btn btn-secondary">
                    </form>
                {{/if}}
            </div>
            <div class="col col-lg-3 my-auto"></div>
        </div>
        {{#if user}}
            <div class="row justify-content-md-center">
                <div class="col col-lg-2">
                    <p class="h2">{{user.username}}</p>
                </div>
                <div class="col col-lg-2">
                    <p class="h2">Level {{user.playerLevel}}</p>
                </div>
                <div class="col col-lg-2">
                    <p class="h2">Win streak: {{user.winStreak}}</p>
                </div>
            <nav class="nav justify-content-center my-3">
                <a href="/collection" class="nav-item btn btn-primary mx-3">Collection</a>
                <a href="/battle" class="nav-item btn btn-primary mx-3">Battle</a>
                <a href="/gacha" class="nav-item btn btn-primary mx-3">Gacha</a>
            </nav>
        {{/if}}
    </div>
</head>
<body>
    {{{body}}}
</body>